{
  "jnss": "https://github.com/eduhds/JsonScripts",
  "version": 1,
  "variables": {
    "app": "rrweb-server"
  },
  "scripts": {
    "run": "g++ main.cpp -o {{app}} && ./{{app}}",
    "build": "cosmoc++ main.cpp -o {{app}}.com",
    "start": "./{{app}}.com"
  }
}
